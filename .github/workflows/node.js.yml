name: Build and Commit Vite Assets

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true  # needed for push

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create .env file (if not committed)
        run: |
          echo "APP_ENV=production" > .env
          echo "APP_KEY=base64:dummykeyhere123456=" >> .env
          echo "APP_URL=https://app.example.com" >> .env

      - name: Build Vite assets
        run: NODE_ENV=production npm run build

      - name: Commit and push public/build to main branch
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add public/build
          git commit -m "CI: Add public/build assets [skip ci]" || echo "No changes to commit"
          git push origin main
